Build/

# Claude (local, per-developer — never commit)
.claude/settings.local.json
.claude/skills/
.mcp.json

# OpenCode (plugin-generated, per-developer — never commit)
**/.opencode/skills/

# IDE workspace folders (per-developer)
.vscode/
.idea/
